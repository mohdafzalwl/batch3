var myage = Number('0o36');

var nextAge = Number('39');

var CatAge = Number('n/a');

myage = 'my age text';

CatAge === CatAge;

isNaN(myage); //true

isNaN(CatAge); //true

isNaN('some randonn age ');

Number.isNan(CatAge);

Number.isNaN('some random text is not a number');

// NAN : Invalid Number
// dont : undefined
